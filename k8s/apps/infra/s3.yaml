apiVersion: s3.aws.upbound.io/v1beta1
kind: Bucket
metadata:
  labels:
    testing.upbound.io/example-name: s3
  name: 14a-deploy-eks-test-bucket-s3
spec:
  forProvider:
    objectLockEnabled: true
    region: eu-central-1
    tags:
      Name: SampleBucket
  providerConfigRef:
    name: aws-config